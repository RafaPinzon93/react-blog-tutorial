This project is from a tutorial about react-redux.

The app get posts and users from an API called jsonPlaceholder. The idea was to make the fewer requests to the API to get the users from the posts.
